schema {
    query: Query
}

type Query {
    nivoBarData(months:[String], validity:[String], sellType: [String]): [NivoBarData]
}

type NivoBarData {
    month: String
    adults: Int
    seniors: Int
    juniors: Int
    portable: Int
    students: Int
}