schema {
    query: Query
}

type Query {
    nivoBarData(months: [String]): [NivoBarData]
}

type NivoBarData {
    month: String
    adults: Int
    seniors: Int
    juniors: Int
    portable: Int
    students: Int
}